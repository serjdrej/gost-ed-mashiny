# Отложенная работа по этому скиллу

Задачи для того, кто возьмёт `gost-ed-mashiny` следующим. Кладутся в
репозиторий, а не в переписку сессий: просьба, живущая в чужом контексте,
живёт до ближайшего компакта.

---

## Дефект: `gost_lookup.py` молчит о том, что его данные — снимок 2024 года

**Оставил:** ведущая сессия маркетплейса `ru-legal-skills`, 2026-09-19.
**Статус:** не начато. Скрипт работает, данные не врут — молчит вывод.

### Факт

`scripts/gost_lookup.py`, строки 34 и 43: URL обоих каталогов прибит на
`data-20240808-structure-20220330.csv` — снимок открытых данных Росстандарта
от **8 августа 2024 года**.

Поиска `2024|snapshot|снимок|дата выгрузки|caveat|note` по файлу — ноль
совпадений. То есть **нигде в выводе дата снимка не названа.**

### Чего здесь нет: вашей вины в устаревании

Проверено 2026-09-19 по странице открытых данных: опубликованы снимки
`20220218, 20220330, 20230912, 20240508, 20240808`. **`20240808` — самый
свежий существующий.** Источник сам не публикует больше года; скрипт прибит
к последнему доступному, а не к забытому.

### Почему это всё равно дефект

Следствие выводится из даты, его не надо измерять: снимок от августа 2024
не может содержать стандарт, принятый позже, и не может отразить отмену,
случившуюся позже. Значит:

- `status` по новому ГОСТу вернёт «не найдено» — **неотличимо от
  несуществующего**;
- `status` по отменённому в 2025–2026 вернёт «Действующий».

При этом `SKILL.md` направляет агента сюда как к живой проверке. Живой в
ней остаётся запрос, но не содержание, и вывод об этом не предупреждает.

Это ровно дефект §8 общей конвенции (`ru-legal-skills/docs/norms-verification-convention.md`):
**поле сообщает о том, что сделал инструмент** («прочитал снимок
2024-08-08»), **а читается как свойство мира** («стандарт действует»).

### Два соображения к починке — решение ваше

1. **Обнаруживать последний снимок на странице, а не прибивать гвоздём.**
   Тогда скрипт починится сам в день, когда Росстандарт опубликует новый, и
   не потребует чьей-либо сессии.
2. **Поле с датой снимка в выводе**, по образцу `arbitrazh-ru`,
   `scripts/egrul_lookup.py:STATUS_CAVEAT` — замер с датой и с двумя ИНН, по
   которым воспроизводится. Он стоит в §8 конвенции как рабочий пример.
   Требование §8: измеренное значение несёт дату **либо** команду
   воспроизведения, причём вторая ветка основная — дата иногда
   невосстановима, команда конструируется всегда.

Цена: строки в скрипте. `SKILL.md` не трогается.

---

## Предупреждение: `claims_scan.py` из `arbitrazh-ru` сюда переносить не надо

Прогнан по ГОСТ-корпусу отдельным анализирующим агентом 2026-09-19:
результат **ложно-чистый** — `ok: True`, ноль непонятого, при трёх нормах,
отсутствующих в таблице. Положительный контроль (добавленная «ст. 15 ГК РФ»)
краснеет, то есть ноль есть свойство прибора, а не свойство корпуса.

**Ведущей сессией маркетплейса этот прогон лично не перепроверялся** —
записано как чужой замер с указанием источника, по требованию §8. Но порог
для переноса он уже не проходит: линтер, зеленеющий на том, чего не
проверял, вреднее отсутствия линтера.

---

## Рекомендация: правило «Как править этот файл» в `SKILL.md`

**Оставил:** ведущая сессия маркетплейса, 2026-09-19, по поручению
владельца. **Рекомендация, не наряд** — обоснованный отказ ценится наравне
с согласием.

533 знака, 9 строк, как стоит у автора (`arbitrazh-ru`):

> ## Как править этот файл
>
> Новая оговорка пишется туда, где будет читатель в момент ошибки. Носители
> по убыванию предпочтения: **(1)** поле в выводе скрипта — оно приходит с
> данными и закрывается тестом; **(2)** файл в `references/`, который маршрут
> обязывает открыть; **(3)** этот файл. Выбор (3) требует назвать, почему (1)
> и (2) не подходят. Поле не может нести то, что нужно **до** вызова: агент,
> не вызвавший скрипт, поля не увидел. Этот файл вырос с 225 строк до 498 за
> один день, и вернётся туда же, если правило не соблюдать.

**Не дубль §10 конвенции:** те же ступени там адресованы сопровождающему,
читающему её изредка; здесь правило стоит там, где агент находится **в
момент правки**.

**Основание для этого репозитория — слабейшее из трёх, и это честная часть
картины.** Проверено 2026-09-19:

```
git show $(git log --diff-filter=A --format=%H -- SKILL.md | tail -1):SKILL.md | wc -c
wc -c < SKILL.md
```

7 622 → 19 688 байт, **×2,6** — самый медленный рост из четырёх
(`legal-ru` ×8,6, `arbitrazh-ru` ×9,3). Довод «иначе распухнет» применим
здесь слабее всех.

**Зато починка `gost_lookup.py` от 2026-09-19 — лучшая в семье иллюстрация
первой ступени.** Оговорка про снимок каталога легла не в `SKILL.md`, а в
поле вывода: с датой, URL и способом получения. Приходит с данными,
закрывается тестом, стоит ноль знаков в файле, который грузится при каждой
активации. Правило описывает то, что здесь уже сделано верно.

**И поправка к распространённому представлению об этом скилле:** его
`SKILL.md` — **самый длинный из трёх соседей по строкам** (213 против 168 и
149), хотя по знакам все трое расходятся на 2 %. «Самый маленький скилл» —
неверная картина, и при решении она не должна играть.

**Переносить последнюю фразу как есть нельзя:** «225 → 498 строк» — факт про
`arbitrazh-ru`. По §8 на её месте собственное число (команда выше) либо та же
цифра с источником и датой.
